package biz.uniquegood.realworld.sunguard.ar.module

import android.content.Context

enum class AROverlayImageType {
    SINGLE_IMAGE, GIF_IMAGE
}

class AROverlayImage(private val context: Context, private val path: String) {

}